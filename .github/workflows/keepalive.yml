name: Supabase Keep-Alive

on:
  schedule:
    - cron: '0 8 */3 * *'

  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping Supabase database
        run: |
          curl --fail --silent --show-error \
            -H "apikey: ${{ secrets.SUPABASE_KEY }}" \
            "https://xvdexhbasqbhvsuitucr.supabase.co/rest/v1/mimos?select=id&limit=1"